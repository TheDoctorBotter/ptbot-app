# Git Secrets Configuration
# This file helps detect secrets in your repository

# Common API key patterns to detect:
# - OpenAI API keys: sk-proj-* or sk-*
# - Google/YouTube API keys: AIza*
# - Resend API keys: re_*
# - Generic tokens and secrets

# To manually scan your repo:
# git log --all --full-history -- .env
# git log --all --full-history -S "sk-proj-" --source --all